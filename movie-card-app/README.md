# MovieCard App

Reusable MovieCard component built with React + Vite.  
Displays movie poster, title, release year, and rating using sample static data.

## Features
- 🎬 Reusable MovieCard component
- 🖼 Poster, title, release year, rating
- 🎨 Styled with CSS (or Tailwind option)
- ⚡ Concepts: props, component reusability, clean styling

## Run locally
1. Install dependencies:
   ```bash
   npm install